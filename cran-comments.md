## Test environments

* local macOS 26.6, R 4.6.1
* GitHub Actions
  * macOS-latest, R release
  * windows-latest, R release
  * ubuntu-latest, R devel, release and oldrel-1
  * ubuntu-latest, R release, hard dependencies only (no Suggests)

## R CMD check results

0 errors | 0 warnings | 1 note

* This is a new release.

## Method references

There are no published references describing the methods in this package.
It generates new data frames of predictor values for use with the prediction
methods of fitted model objects.

## Notes for the reviewer

The CRAN status badge in README.md links to
<https://cran.r-project.org/package=newdata>, which does not resolve until the
package is published.
